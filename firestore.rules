rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // workLogs: iedereen mag lezen, alleen ingelogde gebruikers mogen schrijven
    match /workLogs/{logId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }

  }
}
